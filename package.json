{
  "name": "aher-vet",
  "version": "0.2.0",
  "private": true,
  "type": "module",
  "scripts": {
    "build": "tsx scripts/build.ts",
    "dev": "tsx scripts/build.ts --dev",
    "dev:wrangler": "npm run build && wrangler dev",
    "preview": "npm run build && wrangler dev",
    "deploy": "npm run build && wrangler deploy"
  },
  "browserslist": "defaults, not ie <= 11",
  "dependencies": {
    "motion": "^12.38.0",
    "tailwindcss": "^3.4.17"
  },
  "devDependencies": {
    "@types/node": "^22.10.0",
    "autoprefixer": "^10.4.20",
    "esbuild": "^0.24.0",
    "prettier": "^3.6.0",
    "prettier-plugin-tailwindcss": "^0.6.11",
    "tsx": "^4.19.2",
    "typescript": "^5.9.0",
    "wrangler": "^4.87.0"
  }
}
